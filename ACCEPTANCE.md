# PyPad 验收标准

> **红线**：禁止 Mock 冒充真实逻辑、禁止硬编码 JSON 伪造结果、禁止篡改测试断言。

## 完成定义 (DoD)

任务必须同时满足才能标记 `DONE`：

1. 核心功能代码完整
2. 前后端打通，依赖正常交互
3. 单元测试与 API 测试通过
4. 异常边界有健全的错误处理
5. 文档与 PROGRESS.md 同步更新
6. 验收标准逐条满足
7. E2E 真实环境验证通过

## 禁止行为

- 空函数返回 `{"status": "ok"}` 冒充实现
- 硬编码 JSON 冒充数据库/LLM 结果
- `# TODO` 占位却标记完成
- 只写前端没有后端，或反之
- 裸 `except: pass` 吞掉错误

## 证据链

每次标记 DONE 必须提供：代码文件路径、测试日志、Git Commit ID。
